apiVersion: v1
kind: Service
metadata:
  name: {{ include "flix-server.fullname" . }}
  labels:
    {{- include "flix-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flix-server.selectorLabels" . | nindent 4 }}
